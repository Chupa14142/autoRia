version: '3'
services:
    cypress_test:
              image: cypress_test
              build:
                    context: .
                    dockerfile: ./Dockerfile
              volumes:
                - ./dockerData/:/app/parsedData